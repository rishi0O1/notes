package coding.java.arrays;
import java.util.*;

// The size of the array cannot be altered(once initialized).  
// However, an array reference can be made to point to another array.
public class index {
    // print array as string 
    public static void main(String[] args) {
        int[] arr = new int[6];  // we need to initialise the array : mandatory hai 

        System.out.println(Arrays.toString(arr));
    }
}
